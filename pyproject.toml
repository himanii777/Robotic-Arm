[project]
name = "robotic-arm-hand-tracking"
version = "0.1.0"
description = "Webcam hand-tracking prototype for robotic arm visualization and control."
readme = "README.md"
requires-python = ">=3.9,<3.12"
dependencies = [
    "mediapipe==0.10.9",
    "numpy==1.26.4",
]

[tool.uv]
package = false

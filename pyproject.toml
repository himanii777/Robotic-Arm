[project]
name = "local-hand-tracking"
version = "0.1.0"
description = "Small webcam hand-tracking demo using MediaPipe and OpenCV."
readme = "README.md"
requires-python = ">=3.9,<3.12"
dependencies = [
    "mediapipe==0.10.9",
    "numpy==1.26.4",
]

[tool.uv]
package = false
